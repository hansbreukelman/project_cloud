import * as cdk from 'aws-cdk-lib';
import { CustomResource } from 'aws-cdk-lib';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';
export interface CreateAMICustomResourceProps extends cdk.ResourceProps {
    readonly properties: {
        [propname: string]: any;
    };
    readonly uid: string;
}
export declare class CreateAMICustomResource extends Construct {
    readonly lambda: lambda.IFunction;
    readonly createAMICustomResource: CustomResource;
    constructor(scope: Construct, id: string, props: CreateAMICustomResourceProps);
    private ensureLambda;
}
